services:
  - type: web
    name: Emotion-Detector-1
    env: python
    pythonVersion: 3.10.12
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    branch: main
    region: oregon
